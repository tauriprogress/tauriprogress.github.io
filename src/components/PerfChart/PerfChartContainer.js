import React from "react";
import Container from "@material-ui/core/Container";

function PerfChartContainer({ children }) {
    return <Container>{children}</Container>;
}

export default PerfChartContainer;
