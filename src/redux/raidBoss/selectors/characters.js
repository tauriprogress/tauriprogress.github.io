export function raidBossCharactersEntireSelector(state) {
    return state.raidBoss.characters;
}

export function raidBossCharactersDataSpecificationStringSelector(state) {
    return state.raidBoss.characters.dataSpecificationString;
}
