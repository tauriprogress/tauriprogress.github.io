export function raidBossRecentKillsEntireSelector(state) {
    return state.raidBoss.recentKills;
}

export function raidBossRecentKillstDataSpecificationStringSelector(state) {
    return state.raidBoss.recentKills.dataSpecificationString;
}
